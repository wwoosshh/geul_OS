# Cargo workspace 설정
#
# Linux musl cross-compile은 Windows host에서 native gcc-musl 없이도
# rust-lld(LLVM linker)로 처리. M6 (VM 부팅) 빌드 의존.

[target.x86_64-unknown-linux-musl]
linker = "rust-lld"
rustflags = ["-C", "link-self-contained=yes"]
